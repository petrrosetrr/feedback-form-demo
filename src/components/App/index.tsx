import React from 'react';
import FeedbackForm from "../Form";
import './index.scss';

const App = () => {
    return (
        <div className={'app'}>
            <FeedbackForm/>
        </div>
    );
};

export default App;
